#include<bits/stdc++.h>
using namespace std;

int main()
{
    //freopen("f.txt","r",stdin);
    int t;
    cin>>t;
    string s[53];
    for(int cas=1; cas<=t; cas++)
    {
        deque<string>dq;
        for(int i=0; i<52; i++)
        {
            cin>>s[i];
            if(i<25)
                dq.push_front(s[i]);
        }
        int y=0;
        for(int j=0;j<3;j++)
        {
            string x=dq.front();
            dq.pop_front();
            int val=10;
            if(x[0]>='2'&&x[0]<='9')
                val=x[0]-'0';
            y+=val;
            for(int i=0;i<(10-val);i++)
                dq.pop_front();
        }
        for(int i=25;i<50;i++)
            dq.push_front(s[i]);
        for(int i=1;i<y;i++)
            dq.pop_back();
        cout<<"Case "<<cas<<": "<<dq.back()<<endl;
    }
    return 0;
}
